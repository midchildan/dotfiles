This directory contains patches to support non-standard or legacy environments.
Apply these patches on your local branch if needed.

If patches don't apply with "patch -p1" or "git apply -3", try running
recountdiff(1) on the patch files to fix the offsets.
