# The included files should be sorted by filename in lexicographical order.  If
# there are conflicting settings, whichever comes first should take precedence.
# See glob(3) and ssh_config(5) for details
Include ~/.ssh/config.d/*.conf
